def init():
    # Internal Parameters
    global C, g, L, T, H, a, m1, m2, m3, m4, beta, tau_w

    # Connection Parameters
    global G_gap, G_syne, E_syne, G_syni, E_syni, k_syn, a, b
    
    # Network Parameters
    global numCells, OscInds, PassiveInds, GJconn, Econn, Iconn